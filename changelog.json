{
  "version": "1.0.3",
  "dmg_required": false,
  "changes": [
    "/update view — changelog, progress bar, version info",
    "Rainbow border pulse when update is available",
    "SSL fix for update checks in py2app bundle",
    "DMG installer with drag-to-install layout"
  ]
}
